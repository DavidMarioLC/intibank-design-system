---
"intibank-ui": minor
---

Add `Input` (sizes: sm, default, lg; disabled and invalid states) and the `Field` compound (`Field.Root`, `Field.Label`, `Field.Description`, `Field.Error`), both built on `@base-ui/react`. `Field` wires the label, accessible description and validation state to any control, so `Input` stays a pure control.
